#include <iostream>

using namespace std;

int main(int argc, char const *argv[]){
	int count = 5;
	{
		for(unsigned i = 0; i < count; ++i) {
 	/* code */
		}
		for(unsigned i = 0; i < count; ++i) {
			while(1) {	
				int i = 0;
				cout <<"ww/* "<<endl;
				break;
			}
		}
	}
}
